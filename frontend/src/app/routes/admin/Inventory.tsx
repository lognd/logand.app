// TODO(logan): inventory item list/create/move, location management,
// tag + full-text search, see docs/design/06-inventory.md.
export function AdminInventory() {
  return (
    <main>
      <h1>Inventory (admin)</h1>
    </main>
  );
}
