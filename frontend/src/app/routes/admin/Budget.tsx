// TODO(logan): expense ledger view + evidence upload form, see
// docs/design/05-budget.md. Remember: editing an entry with evidence
// attached must go through the correction flow, not a plain PATCH.
export function AdminBudget() {
  return (
    <main>
      <h1>Budget (admin)</h1>
    </main>
  );
}
