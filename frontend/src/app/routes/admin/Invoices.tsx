// TODO(logan): list/create/send/void invoices via src/api/invoices.ts
// (admin endpoints, see docs/design/04-invoices.md). Needs a data table +
// create-invoice form (React Hook Form + zod).
export function AdminInvoices() {
  return (
    <main>
      <h1>Invoices (admin)</h1>
    </main>
  );
}
