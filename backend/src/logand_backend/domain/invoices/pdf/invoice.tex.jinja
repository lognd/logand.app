\documentclass{logandinvoice}

\companyname{\VAR{business_name}}
\companydetails{\VAR{business_details}}
\invoicenumber{\VAR{invoice_number}}
\invoicedatevalue{\VAR{invoice_date}}
\invoiceduedate{\VAR{due_date}}
\invoicestatus{\VAR{status}}
\billto{\VAR{bill_to}}

\hypersetup{pdftitle={Invoice \VAR{invoice_number}}, pdfauthor={\VAR{business_name}}}

\begin{document}
\thispagestyle{fancy}

\invoiceheader
\billtoblock

{\renewcommand{\arraystretch}{1.35}%
\begin{tabularx}{\textwidth}{@{}X r r r@{}}
\invoicetableheader
\BLOCK{ for item in line_items }
\VAR{item.description} & \VAR{item.quantity} & \BLOCK{ if item.unit }\VAR{currency_symbol}\VAR{item.unit_price} / \VAR{item.unit}\BLOCK{ else }\VAR{currency_symbol}\VAR{item.unit_price}\BLOCK{ endif } & \VAR{currency_symbol}\VAR{item.amount} \\
\BLOCK{ endfor }
\bottomrule
\end{tabularx}}

\invoicetotals{%
\begin{tabular}{@{}r r@{}}
\textbf{Total (\VAR{currency_upper}):} & \textbf{\VAR{currency_symbol}\VAR{amount_total}} \\
\end{tabular}%
}

\vspace{24pt}

\textbf{\color{invoiceaccent}Payment}

\vspace{6pt}
\noindent
\BLOCK{ if pay_url }
Pay online: \href{\VAR{pay_url}}{\VAR{pay_url}}\\[6pt]
\BLOCK{ endif }
\#{ KEEP IN SYNC: this "Alternative payment methods accepted" list and
the customer Pay page's "Other ways to pay" box
(frontend/src/app/routes/customer/Pay.tsx) show the SAME manual payment
methods (Zelle handle, PayPal receive email, in-person) in the SAME
style -- a bold method label with the handle/email set off in monospace,
distinct from surrounding prose. Whenever you change one -- add a method,
reword it, restyle how a handle is shown -- update the other in the same
change so a customer sees consistent options on the PDF and the Pay page. }
Alternative payment methods accepted:
\begin{itemize}
\setlength{\itemsep}{2pt}\setlength{\parskip}{0pt}\setlength{\topsep}{4pt}
  \item \textbf{Zelle}\BLOCK{ if zelle_handle }\quad\texttt{\VAR{zelle_handle}}\BLOCK{ endif }
  \item \textbf{PayPal}\BLOCK{ if paypal_receive_email }\quad\texttt{\VAR{paypal_receive_email}}\BLOCK{ endif }
  \item \textbf{In person}\quad by prior arrangement
\end{itemize}
To use one of these instead, contact
\href{mailto:\VAR{contact_email}}{\VAR{contact_email}}.

\BLOCK{ if memo }
\vspace{18pt}
\textbf{\color{invoiceaccent}Notes}

\vspace{6pt}
\noindent \VAR{memo}
\BLOCK{ endif }

\end{document}
